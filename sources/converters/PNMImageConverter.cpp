#include "headers/converters/PNMImageConverter.h"

QImage PNMImageConverter::convertImage(const QImage& image, const QString& format, const QString& filePath) {
    QImage img = image;
    return image;
}
